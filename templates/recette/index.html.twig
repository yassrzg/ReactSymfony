{% extends 'base.html.twig' %}

{% block title %}Hello RecetteUserController!{% endblock %}

{% block body %}

    <div id="titleRecette" class="reveal-1">
        <h2 class="reveal-1">Nos Recettes</h2>
        <p class="reveal-2">Découvrez nos délicieuses recettes adaptées à différents régimes alimentaires. Que vous suiviez un régime végétarien, végétalien, sans gluten ou autre, nous avons des options savoureuses qui combleront vos papilles. Cependant, veuillez prendre note des précautions liées aux allergènes. Votre sécurité est primordiale, donc assurez-vous de vérifier attentivement les ingrédients utilisés dans nos recettes. Consultez un professionnel de la santé en cas de doute ou d'allergies spécifiques. Explorez nos recettes saines et savoureuses, conçues pour s'adapter à votre régime, et profitez d'une expérience culinaire sans compromis. Bon appétit !</p>
    </div>
    <div id="container-Recette">
        {% for recette in recettes %}
            <a href="{{ path('app_recette_id', { 'id': recette.id }) }}" id="link-recette">
                <div class="containerRecette">
                    <div class="container-img">
                        <img src="/Uploads/{{ recette.imageRecette }}" alt="Recette" class="imgRecette"/>
                    </div>
                    <div class="container-description">
                        <h2>{{ recette.titre }}</h2>
                        <div class="container-time">
                            <div class="time">
                                <img src="{{  asset('Image/icons-sablier.png') }}" alt="iconSablier" />
                                <p>Préparation:</p>
                                <p>{{ recette.tempsPreparation|date('H:i') }} heure</p>
                            </div>
                            <div class="time">
                                <img src="{{  asset('Image/icons-minuteur.png') }}" alt="minuteurImage" />
                                <p>Cuisson:</p>
                                <p>{{ recette.tempsCuisson|date('H:i') }} heure</p>
                            </div>
                        </div>
                        <div id="allergieRecette">
                            <div class="allergie">
                                <img src="{{ asset('Image/icons-attention.png') }}" alt="attention">
                                <p>ALLERGIE</p>
                            </div>
                            <div class="container-allergie">
                                {% for allergie in recette.Allergie %}
                                    <p>{{ allergie.name }}</p>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                    <div>
                        <div {{ react_component('RatingStars', { noteMoyenne: recette.NoteMoyenne }) }} class="rating-recette"></div>
                    </div>
                </div>
            </a>

        {% endfor %}


        {#                <div {{ react_component('RatingStars') }}"></div>#}
    </div>









{% endblock %}
